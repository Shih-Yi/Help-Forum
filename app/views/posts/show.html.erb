<p>Category: <%= @post.category.try(:name) %><p>

<h3>View count:<%= @post.post_count %></h3>

<p>Group:
<% @post.groups.each do |g| %>
  <%= g.name %>
<% end %>
</p>
<h3>Title:</h3>
<div><%= @post.name %></div>
<h4>Context:</h4>
<div>
  <p><%=simple_format @post.description %></p>
</div>

<div id="rateYo"></div>
<button class="button">give reviews</button>
<p><%#= link_to "give reviews", new_post_comment_path(@post, @review) %></p>

<%= link_to "Back to homepage", posts_path %>

<h4><%= @post.try(:comment_number) %>Comments</h4>

<div class="panel-footer">
  <%= render :partial=> 'post_comments/comment' %>
</div>

</div>
<h4>Add a comment:</h4>
<%= render "post_comments/form" %>


<script>
  $(function () {

  var $rateYo = $("#rateYo").rateYo();

  $(".button").click(function () {
    /* get rating */
    var rating = $rateYo.rateYo("rating");
    console.log(rating);


      var post_id =  <%= @post.id %>;
      $.ajax({
        type: 'POST',
        url: "/posts/" + post_id + "/reviews",
        dataType: 'JSON',
        data: {
          rating: rating
        },
        success: function(){
        }
      });


  });
});

</script>
